import React from 'react';
import {observer} from "mobx-react-lite";
import {Container} from '@chakra-ui/react';

export const VehiclesList = observer(() => {
    return(<Container>Vehicles</Container>);
});