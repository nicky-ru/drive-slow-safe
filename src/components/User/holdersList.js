import React from 'react';
import {observer} from "mobx-react-lite";
import {Container} from '@chakra-ui/react';

export const HoldersList = observer(() => {
    return(<Container>Holders</Container>);
});