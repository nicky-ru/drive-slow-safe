import React from 'react';
import {observer} from "mobx-react-lite";
import {Container} from '@chakra-ui/react';

export const ClaimsList = observer(() => {
    return(<Container>Claims</Container>);
});